## Code

- [Git](https://git-scm.com) (ie. from [brew](https://brew.sh))

    ```
    $ brew install git
    ```
    ---
    ```
    $ mkdir -p ~/repos && cd ~/repos \
    && git clone git@github.com:Blazemeter/private-cloud.git \
    && cd private-cloud
    ```
