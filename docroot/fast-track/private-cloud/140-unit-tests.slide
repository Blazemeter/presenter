## Unit Tests

- Use `EnvFile` **`ut.env`**
- In project's file tree right-click directories
    - `Mark Directory as: Sources Root`
        - `app`
        - `bzm_app`
        - `private_cloud`
    - `Mark Directory as: Test Sources Root`
        - `tests`
    - `Mark Directory as: Excluded`
        - `venv`
